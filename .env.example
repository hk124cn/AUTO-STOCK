# AUTO-STOCK 密钥模板
# 复制为 .env 并填入实际值。.env 已加 .gitignore，永远不会进 git。
# 每台机器（本地 / 云服务器）各自维护自己的 .env。

# tushare.pro 个人中心 → token
# 注意：旧 token 已 commit 到 git 历史，请去 tushare.pro 重置后填新值
TUSHARE_TOKEN=

# 持仓/统计/策略页面的实操密码
# 用户在 stock.auto-claw.top 点"买入"等按钮时弹密码框，输此密码可获 24h token
API_PASSWORD=
